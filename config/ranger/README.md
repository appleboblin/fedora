Creates config in ~/.config/ranger  
`$ ranger --copy-config=all`

Sets color scheme:  
Creates config file in ~/.config/ranger `$ touch .colorschemes`  
`set colorscheme solarized`  
Show hidden files `rc.conf`  
`set show_hidden true`
